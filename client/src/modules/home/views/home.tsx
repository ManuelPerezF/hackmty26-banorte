import { HomeOverview } from '../components/homeOverview';
import type { HomeProps } from '../types/home.types';
export function AccountsOverview(props: HomeProps) {
  return <HomeOverview {...props} />;
}
